import React from "react";
import { FullScreenCentered, LoadingIcon } from "./Loading.styled";

const Loading = () => {
  return (
    <FullScreenCentered>
      <LoadingIcon />
    </FullScreenCentered>
  );
};

export default Loading;
